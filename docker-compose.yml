services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: catcafe
      POSTGRES_USER: catcafe
      POSTGRES_PASSWORD: catcafe
    ports: ["5432:5432"]
    volumes: ["catcafe-db:/var/lib/postgresql/data"]

volumes:
  catcafe-db:

